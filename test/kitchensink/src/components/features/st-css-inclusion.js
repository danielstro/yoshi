import React from 'react';
import styles from './assets/style.st.css';

export default () => (
  <div>
    <p id="st-css-inclusion" className={styles.root}>
      Stylable is working!
    </p>
  </div>
);
